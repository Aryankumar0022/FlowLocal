[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flowlocal-shared"
version = "0.1.0"
description = "Shared utilities for FlowLocal AI services"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["flowlocal_shared"]
